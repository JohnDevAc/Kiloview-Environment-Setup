# Kiloview Environment Setup 2.1.2

Setup now gates package downloads against the sources needed for the chosen role and permits verified cached packages when offline. Client-only setup does not install local server components. Durable component receipts preserve deployment ownership, unsupported receipts cannot be silently replaced, and completed restoration residue no longer claims a server is installed.

PC Agent packages are validated as a complete pair. Client readiness rejects incomplete or unusable endpoint/network identity. Shared NDI runtime removal respects existing consumers and recovery data remains available after interrupted work.

Validation: 95 isolated regression checks passed and the setup executable was rebuilt.
